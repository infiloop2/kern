"""Agent harnesses, provider trust, and live-turn orchestration.

This package runs inside ``kern-admin`` but is independent of the admin HTTP
and Workspace socket transports in ``host.runtime.admin_api``.
"""
