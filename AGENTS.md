# Repository agent guidance

## Tests

- Run unit tests through `tests/scripts/test` instead of creating and
  populating a new environment. With no arguments it runs the full suite; pass
  an ordinary unittest module or test name for a focused run.

## Pull requests

- Whenever you create a pull request, enable auto-merge immediately so it
  merges after its required checks and reviews pass.
